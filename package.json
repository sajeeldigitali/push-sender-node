{
  "name": "push-sender-node",
  "version": "1.0.0",
  "description": "for sending expo notification via node",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "expo-server-sdk": "^3.6.0"
  }
}
